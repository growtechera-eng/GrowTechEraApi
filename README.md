# GrowTechEra API

Minimal Express entry added so `npm start` and `npm run dev` work.

Run locally:

```powershell
npm install
npm start
# or for development with auto-restart
npm run dev
```

Server listens on `PORT` environment variable or `3000` by default.
